"""ball_gpt: character tokenizer + corpus dataset (Phase 2)."""

from ball_gpt.tokenizer import CharTokenizer

# Import CharLMDataset / build_datasets from ball_gpt.dataset (requires torch).

__all__ = ["CharTokenizer"]
